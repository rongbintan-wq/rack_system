# 机柜管理系统 · 能力地图（产品能力总览 · 非开发人员版）

> 本文档只回答一个问题：**「这个系统到底能做什么？」**  
> 写给机房管理员、运维同事、以及任何需要提需求 / 用系统的非开发人员。读一遍，你就能对系统能力建立整体认知，提需求时更「有的放矢」，和开发 / AI 沟通时更精准。  
> 所有内容均基于本机真实系统（版本 **1.3.0**）整理，不臆测。

---

## 一、一句话定位

机柜管理系统 = 一套**网页上的「机房设备台账 + 机柜平面图」**：把过去用 Excel 管理的「机房 → 机柜 → 设备」三级信息，变成网页里能看、能点、能批量导入导出的工具。

---

## 二、先认识三个层级（系统的「骨架」）

系统里的所有数据都按这个层级组织，提需求 / 看页面时心里先有这个数：

| 层级     | 是什么          | 举例                           |
| ------ | ------------ | ---------------------------- |
| **机房** | 最大的区域，一个物理机房 | 「北京机房 A」                     |
| **机柜** | 机房里的一个个铁柜子   | 「A 排 01 号机柜」（柜子有 U 数，常见 42U） |
| **设备** | 柜子里放的具体设备    | 交换机、服务器（占几个 U 位）             |



> 一句话：**一个机房里有多个机柜，一个机柜里放多台设备。**

---

## 三、能力地图（按业务域分类，每条一句话）

### 1. 机房管理

| 能力       | 一句话说明                                 |
| -------- | ------------------------------------- |
| 机房列表查看   | 打开系统第一眼就是所有机房的清单，可看总数、状态              |
| 新建机房     | 在网页上填名字 / 区域就能新增一个机房                  |
| 机房详情查看   | 点进某个机房，能看到它下面所有机柜排布的平面图               |
| 编辑机房     | 改机房名称、区域、状态等信息                        |
| 删除机房     | 删除是「软删除」（数据保留、前台不显示），不会真把数据清空         |
| 机房设备类型统计 | 机房详情页有图（饼图 / 柱状图），一眼看出这机房里交换机几台、服务器几台 |

### 2. 机柜管理（内嵌在机房详情里，没有独立菜单）

| 能力             | 一句话说明                              |
| -------------- | ---------------------------------- |
| **机柜平面图（核心）**  | 每个机柜画成一张 U 位图，哪层放了什么设备、空着哪几 U 一目了然 |
| 设备色块显示         | 图里每块设备显示「品牌 + 型号」文字，颜色按设备类型区分      |
| 机柜占用统计         | 显示这柜子已用多少 U、还剩多少 U                 |
| 新建 / 编辑 / 删除机柜 | 在网页上增改机柜（含 U 数），删除同样是软删除           |

### 3. 设备管理

| 能力      | 一句话说明                              |
| ------- | ---------------------------------- |
| 设备列表查看  | 看某台设备的基本信息（品牌、型号、U 位、状态等）          |
| 上架（放设备） | 点空闲 U 位或填表单，把设备放进某个机柜的某个 U 位       |
| 编辑设备    | 改设备信息，包括挪到别的 U 位 / 机柜（系统会自动检查是否撞位） |
| 下架设备    | 设备退网时点「下架」，释放它占的 U 位（数据保留，可恢复）     |
| 重新上架    | 下架错了？在「已下架设备」面板点重新上架即可（若 U 位被占会提示） |
| 删除设备    | 彻底移除（软删除，数据保留可查）                   |

### 4. Excel 批量数据处理（最常用）

| 能力     | 一句话说明                                      |
| ------ | ------------------------------------------ |
| 下载导入模板 | 系统提供固定 14 列 Excel 模板，照着填就不会错               |
| 导入预览校验 | 上传 Excel 后先「试跑」，绿色 = 将通过，红色 = 哪行错、错在哪，先不写入 |
| 导入提交   | 校验通过后一键导入；只要有一行错，整批都不写，不会半截乱入              |
| 导出全部设备 | 一键把系统里所有设备导出成 Excel（和导入模板列一致，方便回改再导）       |
| 下载示例数据 | 提供一份 10 行示例 Excel，看格式长什么样                  |

> **导入模板 14 列顺序（不要调列、不要少列）**：  
> 资源ID / 资源编号 / 设备类型 / 品牌名称 / 型号 / 区域省份城市场地 / 机房编号或名称 / 机柜名称 / 机柜编号 / 起始U位 / 占用U位数 / 资产状态 / SN序列号（选填）/ 主机名。

### 5. 账号与登录

| 能力     | 一句话说明                                     |
| ------ | ----------------------------------------- |
| 登录     | 输入账号密码进入系统                                |
| 查看当前账号 | 看自己登录的是谁                                  |
| 默认账号   | 系统自带 `admin / admin123`（当前登录校验默认关闭，可能直接进） |

### 6. 系统状态与运维入口

| 能力     | 一句话说明                       |
| ------ | --------------------------- |
| 健康检查   | 一个地址能告诉你「系统活着没、要不要登录」，运维排错用 |
| 文件下载入口 | 模板、示例数据就是通过固定网址直接下载的        |

### 7. 开放数据接口（给技术同事对接用）

> 如果你要把系统数据对接到别的平台 / 报表，系统对外提供标准数据接口。非开发人员知道「有这能力」即可，具体对接交给技术同事。  
> | 接口能拿到什么 | 用途 |  
> |---|---|  
> | 机房数据 | 对接别的系统取机房清单 |  
> | 机柜数据 | 取机柜清单与占用情况 |  
> | 设备数据 | 取设备清单 |  
> | 设备导出 | 程序化下载全部设备 Excel |  
> | 导入（预览 / 提交） | 别的系统也能把 Excel 推进来 |  
> | 机柜平面图数据 | 取 U 位布局，自己画平面图 |

### 8. 本期暂无、但已在规划的能力（路线图）

按项目规划（Phase 2+），以下功能**当前版本还没有**，提需求时请勿假设「已有」：

- 自动发现网络设备（SNMP）
- 容量规划报表
- 对接 CMDB / 网管系统
- 巡检 / 下架审批智能体（Agent）
- 多租户

---

## 四、常用任务 → 用哪个能力（速查表）

| 我想做…             | 应该…                       |
| ---------------- | ------------------------- |
| 看公司有哪些机房         | 打开机房列表                    |
| 看某个机柜里放了啥、空几 U   | 进机房详情 → 看机柜平面图            |
| 把一批设备录进系统        | 下载模板 → 填好 → 导入（先预览校验）     |
| 把系统里设备导出来备份 / 汇报 | 点「导出全部设备」                 |
| 设备上架 / 下架        | 平面图点设备 → 上架 / 下架；下架错可重新上架 |
| 改设备放的 U 位        | 编辑设备，改 U 位（系统会检查是否撞位）     |
| 看某机房设备类型分布       | 机房详情页的类型统计图               |
| 换电脑 / 重装后怎么用     | 见《运维操作手册》启停章节             |

---

## 五、提需求前请先看这里（避免重复提 / 不敢提）

1. **先确认是不是已有能力**：上表列出的都是「已经有了」的。小调整（改文案、加个筛选、调列顺序）直接说；不要提「能不能加个机房列表」这种已有的需求。
2. **描述需求用「产品语言」**：和开发 / AI 沟通时，用下面这些词更准确，减少来回：
   - **导航栏**：页面最上面那条
   - **列表 / 表格**：一排排数据的区域
   - **详情页**：点进去看单个对象的页面
   - **弹窗**：从中间蹦出来的小窗口（如导入框）
   - **抽屉**：从右边滑出来的面板（如设备详情）
   - **面包屑**：页面顶部「机房 > 机柜」这种层级路径
   - **按钮 / 图标按钮**：能点的地方
3. **说清「在哪个页面、哪个区域、想变成什么样」**。例如：「在机房详情页的设备类型统计图下面，我想加一个按品牌分布的图」——这比「加个图」高效十倍。

---

## 六、附录：地址、账号、版本

- **访问地址**：`http://127.0.0.1:8000`（本机浏览器打开）
- **健康检查地址**：`http://127.0.0.1:8000/api/health`
- **模板下载**：`http://127.0.0.1:8000/api/files/template`
- **示例下载**：`http://127.0.0.1:8000/api/files/sample`
- **默认账号**：`admin` ／ 密码 `admin123`
- **当前版本**：1.3.0（详见 `CHANGELOG.md`）
- **相关文档**：`运维操作手册.md`（怎么开关 / 备份 / 排错）、`开发手册.md`、`测试手册.md`

> 备注：本文档基于真实系统功能与接口端点整理（机房 / 机柜 / 设备 / 导入 / 导出 / 认证 / 健康检查 / 文件下载）。如系统升级后能力变化，以每周一自动核对的《运维 / 开发 / 测试 / 能力地图》手册为准。
