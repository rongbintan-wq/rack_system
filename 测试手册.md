# 机柜管理系统 · 测试手册（DCIM-Lite）

> 最后更新：2026-08-12
> 适用版本：v1.3.0（CHANGELOG 最新）
> 阅读对象：测试人员 / 验收人员 / QA
> 本手册所有路径、端口、账号、端点均来自仓库真实文件（`setup.bat`、`app/main.py`、`init_db.py`、`importer_test.py`、`README.md`），照着即可执行，无凭空内容。

---

## 一、测试目标与范围

本系统管理「机房 → 机柜 → 设备」三级资源，核心能力是 **Excel 批量导入 / 导出** 与 **SVG 机柜平面图自动渲染**。测试重点：

1. **可视化正确性**：机柜 SVG 图 U 位渲染、设备色块「品牌+型号」标签、颜色编码。
2. **导入正确性**：模板 14 列、机柜归属、U 越界、U 位冲突、幂等增量、整批回滚。
3. **导出正确性**：全量设备导出、列与模板对齐、中文文件名下载正常。
4. **设备生命周期**：上架 / 下架 / 重新上架，尤其「下架释放 U 位、占用口径统一、重上架占用告警」。
5. **服务可用性**：启动 / 停止 / 健康检查 / 端口占用。

> 不在本期范围（仅记录，不测）：端口、线缆、SNMP 自动发现、容量报表、CMDB 对接（属 Phase 2+）。

---

## 二、测试环境搭建

### 前置依赖（本机已装，禁止删除）
- Python 运行时：`C:\Users\Administrator\.workbuddy\binaries\python\envs\rack\Scripts\python.exe`（3.13.12）
- Node 运行时：`C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2\npm.cmd`（22.22.2）

### 初始化与启动
在 CMD 中进入项目根 `C:\Users\Administrator\rack_system`：
```bat
setup.bat     # 装依赖 + 构建前端 + 初始化数据库（首次/重装必跑）
start.bat     # 启动后端（:8000 托管前端），窗口标题 RackSystem-Backend
```
若提示「端口 8000 已被占用」说明已在运行，直接访问即可；需重启先 `stop.bat` 再 `start.bat`。

### 访问入口
| 用途 | 地址 |
|---|---|
| 前端页面（生产） | `http://127.0.0.1:8000` |
| 前端页面（开发热更新，可选） | `http://127.0.0.1:5173` |
| 后端 API 根 | `http://127.0.0.1:8000/api` |
| 健康检查 | `GET http://127.0.0.1:8000/api/health` |
| 模板下载 | `GET http://127.0.0.1:8000/api/files/template` |
| 示例数据下载 | `GET http://127.0.0.1:8000/api/files/sample` |

### 默认账号
- 用户名 `admin` / 密码 `admin123`（当前 `.env.local` 中 `REQUIRE_AUTH=false`，可能不弹登录框直接进；开启后需用此账号登录）。

> 测试前请确认 `http://127.0.0.1:8000/api/health` 返回含 `"env"` 的 JSON，确认服务活着。

---

## 三、测试数据准备

1. **预置数据**：`init_db.py` 自动写入 3 机房（开放区01/02、荣耀机房）、6 机柜（A01/A02/B01/B02/C01/C02，均 42U）、15 设备。首次 `setup.bat` 即带出。
2. **在线模板**：页面「导入设备」弹窗内下载 `设备导入模板.xlsx`，或 `GET /api/files/template`。
3. **示例数据**：`GET /api/files/sample`（10 行示例），可用于快速导入验证。
4. **数据重置**：停止服务 → 删除 `backend/data/dcim.db` → 重新 `python init_db.py`（或用 `setup.bat`）即可恢复预置数据。
5. **备份建议**：测试前复制 `backend/data/dcim.db` 一份（如 `dcim_test_bak.db`），脏数据可覆盖还原。

---

## 四、功能测试清单（逐条勾选）

### 4.1 机房管理
- [ ] 机房列表页可打开，显示 3 个预置机房（开放区01/02、荣耀机房）。
- [ ] 进入某机房详情，内联渲染该机房全部机柜的 SVG 机柜矩阵（无独立机柜详情菜单）。
- [ ] 机柜图显示设备色块并标注「品牌+型号」；颜色按类型（交换机青、服务器蓝、防火墙橙、路由器紫、存储绿）。
- [ ] 空闲 U 位为浅灰虚线、预留为黄色斜纹。

### 4.2 设备操作
- [ ] 点击设备色块 → 抽屉显示详情（资源编号、类型、品牌、型号、U 位、状态、SN、主机名）。
- [ ] 点击空闲 U 位 → 上架表单可填写并提交；上架后色块出现在正确 U 位。
- [ ] 设备下架：抽屉内「下架」→ 状态变「已下架」，**机柜图中该设备消失、U 位释放**。
- [ ] 重新上架：机房详情「已下架设备」折叠面板 → 点「重新上架」→ 复用原 U 位成功。

### 4.3 Excel 导入
- [ ] 机房列表页与机房详情页均有「📥 导入设备」入口，共用同一弹窗。
- [ ] 下载模板为 14 列（A-O）：资源ID / 资源编号 / 设备类型 / 品牌名称 / 型号 / 区域省份城市场地 / 机房编号或名称 / 机柜名称 / 机柜编号 / 起始U位 / 占用U位数 / 资产状态 / SN序列号（可选）/ 主机名。
- [ ] 上传 → 「预览校验」：绿色区显示「将新增 N / 将更新 M」，红色区列出错误行原因。
- [ ] **成功路径**：一行无错 → 「确认导入」→ 事务写入，设备出现在机柜图。
- [ ] **幂等**：重复导入同一文件（同资源编号）→ 原地 UPDATE，不重复创建。
- [ ] **整批回滚**：任意一行错 → 整批不写入（库不变）。

### 4.4 Excel 导出
- [ ] 机房列表页「📤 导出全部设备」→ 浏览器自动下载 xlsx。
- [ ] 导出文件名中文正常（`设备导出.xlsx`，无乱码 / 无 422）。
- [ ] 导出列与模板 14 列对齐，且包含全部非删除设备。

### 4.5 鉴权（开启后）
- [ ] `REQUIRE_AUTH=true` 时，未登录访问 API 返回 401；登录后正常。

### 4.6 机柜编辑（改名 / 改属性）
- [ ] 机柜卡片右上角出现「✎」编辑按钮（不触发卡片点击）。
- [ ] 点击「✎」弹出「编辑机柜」弹窗，机柜编号只读展示、其余字段（名称/总U数/电源/状态/位置备注/备注）可改。
- [ ] 修改「机柜名称」保存后，卡片标题与机柜矩阵即时更新；同一机房内允许重名（仅编号唯一）。
- [ ] 保存失败（如网络/服务异常）有错误提示且不丢失已填内容。

---

## 五、关键算法与边界用例（必测）

| 用例 | 操作 | 预期结果 |
|---|---|---|
| **U 位冲突** | 在已占用 U 区间（如已有 10-11U）导入/上架 11U 设备 | 报「位置已占用告警」，拒绝写入 |
| **U 越界** | 42U 机柜，起始U=42、占用2U | 报「超出机柜范围」，拒绝 |
| **机柜不存在** | 导入行写不存在的机柜编号 | 报「机柜不存在」，该行失败 |
| **自更新不冲突** | 同资源编号改 U 位重新导入 | 不报冲突，action=update 成功 |
| **下架释放 U** | 下架某设备后，同 U 位上架新设备 | 新设备成功上架（旧已下架不拦） |
| **重上架占用告警** | 旧设备下架后，其 U 被新设备占用，再点旧设备「重新上架」 | 报「位置已占用告警」，禁止上架 |
| **占用口径统一** | 下架设备应不显示在机柜图、不计入占用率 | 四者（渲染/冲突/统计/列表）一致 |
| **路由顺序（导出 422）** | 改代码加 `/export` 后**未重启**直接调 | 旧进程返回 422（验证重启铁律）；`stop.bat`+`start.bat` 后变 200 |
| **API 根路径** | 请求 `/api/v1/rooms` | 404；正确为 `/api/rooms` |
| **中文文件名** | 导出下载 | 文件名 `设备导出.xlsx` 正常，无 latin-1 崩溃 |

---

## 六、接口测试要点（可配合 curl / Postman）

| 接口 | 方法 | 校验点 |
|---|---|---|
| `/api/health` | GET | 返回 `{code:0, data:{env, require_auth}}` |
| `/api/rooms` | GET | 返回机房列表 |
| `/api/racks` | GET | 返回机柜列表 |
| `/api/devices` | GET | 默认只返回占用中设备；`?asset_status=已下架` 返回已下架 |
| `/api/import/preview` | POST | Dry-Run，返回新增/更新/错误分区 |
| `/api/import/commit` | POST | 事务写入，整批原子 |
| `/api/devices/export` | GET | 返回 xlsx（`application/vnd.openxmlformats...`），200 |
| `/api/files/template`、`/api/files/sample` | GET | 返回 xlsx 文件流 |

统一返回结构：`{ "code": 0, "data": {}, "msg": "success" }`，`code ≠ 0` 即错误。

---

## 七、自动化测试（后端 pytest）

覆盖 U 位冲突算法，4 项（`backend/tests/importer_test.py`）：冲突检测、排除自身 UPDATE、越界、机柜不存在。

```bat
cd C:\Users\Administrator\rack_system\backend
C:\Users\Administrator\.workbuddy\binaries\python\envs\rack\Scripts\python.exe -m pytest tests/ -q
```
预期：`4 passed`。任一失败即阻断发版，反馈开发。

> 前端当前无 lint/test 脚本，以 `npm run build` 通过 + 手工功能验证代替。

---

## 八、回归与版本升级验证

每次更新代码（合 PR / 切版本）后必做：
1. `stop.bat` → `start.bat` 重启加载新代码。
2. `GET /api/health` 正常。
3. 抽查 §4 功能清单（机房列表、机柜图、导入、导出、上下架）。
4. 跑 §7 pytest 4 passed。
5. 试一次「导出全部设备」确认能下载。

---

## 九、缺陷定位与反馈模板

**先看哪里**：后端运行窗口（标题 `RackSystem-Backend`）的报错日志；前端按 F12 看 Console / Network。

**反馈给开发时收集**（越快定位越好）：
1. 复现步骤（双击 start.bat / 导入某 Excel / 更新后重启…）。
2. 后端黑窗口报错文字（整段复制或截图）。
3. 浏览器现象截图（白屏 / 报错提示）。
4. 端口占用：`netstat -ano | findstr ":8000"` 输出。
5. 系统版本：`CHANGELOG.md` 顶部版本号（当前 `1.3.0`）。
6. 是否备份、备份位置。

**常见现象速判**：
- 页面白屏 → 多半前端构建/缓存问题（Ctrl+F5；必要时 `npm run build`）。
- 导出 422 → 后端旧进程未重启（`stop.bat`+`start.bat`）。
- 页面能开但无数据 → 服务可能停了 / 没重启 / 缓存（重启 + Ctrl+F5）。
- 导入整批不写 → 看红色错误区，按行改。

---

## 附录：功能测试检查表（可复制打印）

```
版本：_V1.2.0_____   测试人：_rbtan_____   日期：_20260812_____

[yes ] 服务启动（/api/health 正常）
[yes ] 机房列表 3 个预置机房显示
[yes ] 机房详情内联机柜矩阵渲染正确
[yes ] 设备色块「品牌+型号」标签 + 颜色编码正确
[yes] 设备抽屉查看 / 编辑
[yes ] 空闲 U 上架成功且位置正确
[yes ] 设备下架 → 机柜图消失、U 释放
[yes ] 已下架设备「重新上架」复用原 U
[yes ] 导入模板 14 列下载正确
[yes ] 预览校验 新增/更新/错误 分区正确
[yes ] 正常导入写入成功
[未测试 ] 重复导入幂等（不重复创建）
[yes ] 任意一行错整批回滚
[yes ] U 位冲突 → 「位置已占用告警」
[未测试 ] U 越界 → 拒绝并提示
[未测试 ] 机柜不存在 → 该行失败
[未测试 ] 导出全部设备下载成功（中文名正常）
[未测试 ] 导出列与模板对齐
[未测试 ] pytest 4 passed
[已解决-后端冒烟 ] 机柜编辑：卡片✎ → 编辑弹窗 → 改名保存成功（PUT /api/racks/{id}，rack_code 只读；前端按钮待浏览器复测）
[ ] 备注/发现的问题：_原「新建机柜无法编辑、找不到改名菜单」已于 2026-08-12 解决——新增机柜卡片✎编辑按钮（RoomDetailView + RackGrid），调用既有 PUT /api/racks/{id}；TOTEN600_NR14 重名现已可在页面改名。重复名仅编号唯一、名称允许重复，符合设计。_______________
```

