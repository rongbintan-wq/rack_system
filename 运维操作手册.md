> 本文档由系统生成，面向非开发人员。**最后更新：2026-08-12**。系统已配置定期自动核对项目真实配置（路径、端口、账号、脚本、故障排查、版本号），如项目有变动将自动同步本手册，无需手动催促。

# 机柜管理系统 · 运维操作手册（给非开发人员看）

> 本手册只讲「怎么开关、怎么备份、出问题怎么办」，不涉及任何写代码内容。所有路径、端口、账号都来自本机真实配置，照着做即可。

---

## 一、系统简介

这套系统是用来管理公司**机房、机柜、里面设备**的一套网页工具。过去大家用 Excel 表格记机房里哪个柜子、哪台交换机服务器放在第几 U 位，又慢又容易看错。本系统把这些信息搬到一个网页里，打开就能看到每个机柜的平面图，哪一层放了什么设备、空着哪几 U 一眼就清楚。

常用的核心功能有：
- **看机房和机柜**：网页上列出所有机房，点进去能看到机柜的立体示意图，设备上架、下架一目了然。
- **批量导入/导出 Excel**：把设备信息整理成固定格式的 Excel，一键导入系统；也能把系统里的全部设备一键导出成 Excel 备份。
- **设备上下架**：在网页上点一下就能把设备上架到某个 U 位，或下架释放位置；下架错了还能重新上架。

一句话：**它就是一个放在网页上的「机房设备台账 + 机柜平面图」**。

---

## 二、运行环境说明

### 1. 支持的操作系统
- **Windows（本机就是，主力使用）**：直接双击几个 `.bat` 脚本即可，不需要额外装东西。
- **Linux / macOS（仅简单提及）**：系统也带了对应的 `scripts/setup.sh`、`start.sh` 脚本，但需要自己把运行环境放到系统 PATH 里，一般交给开发人员操作，本手册不展开。

### 2. 哪些环境不能删、不能改（重要）
本系统的运行环境装在系统的一个托管目录里，被启动脚本用**绝对路径**写死了。一旦删除或移动，系统就起不来了：
- **Python 运行环境**：`C:\Users\Administrator\.workbuddy\binaries\python\envs\rack\`（注意：在 `rack_system` 文件夹**外面**）
- **Node 运行环境**：`C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2\`（同样在系统文件夹里，不在项目内）

> **切记：上面这两个目录绝对不要手动删除、移动或修改**，否则所有启动脚本都会失效。

### 3. 系统核心目录说明

| 路径 | 作用 | 是否能动 |
|---|---|---|
| `C:\Users\Administrator\rack_system\` | 系统根目录，所有文件都在这下面 | 整体不要乱删 |
| `…\rack_system\start.bat` | **启动脚本**，双击就能开系统 | **请勿随意修改** |
| `…\rack_system\stop.bat` | **停止脚本**，双击就能关系统 | **请勿随意修改** |
| `…\rack_system\setup.bat` | **初始化/安装脚本**，首次或更新后运行 | **请勿随意修改** |
| `…\rack_system\backend\` | 系统后台程序所在目录 | 不要改里面文件 |
| `…\rack_system\backend\data\dcim.db` | **数据库文件（所有机房/设备数据都存在这里）** | **绝对不能删、不能用别的工具打开乱改** |
| `…\rack_system\backend\.env.local` | **配置文件（含登录账号密码）** | **请勿随意修改** |
| `…\rack_system\backend\.env.example` | 配置模板（给开发人员参考） | 不要改 |
| `…\rack_system\web\` | 网页前端程序目录 | 不要改 |
| `…\rack_system\web\dist\` | 编译好的网页页面（由 setup.bat 生成） | 不要手改，重装会自动生成 |
| `C:\Users\Administrator\.workbuddy\binaries\…` | 系统运行环境（见上） | **绝对不能删** |

---

## 三、系统启停操作（Windows）

> **关键提醒**：启动后屏幕会弹出一个名叫 **`RackSystem-Backend`** 的黑色窗口，这是系统在运行。**这个窗口不能关**，关了系统就停了。

### 1. 启动系统
**第一步**：打开 `C:\Users\Administrator\rack_system\` 文件夹。
**第二步**：找到 `start.bat`，**双击**它。
**第三步**：会弹出一个黑色命令窗口（标题含 `RackSystem-Backend`）。稍等几秒，窗口里出现类似 `Application startup complete` 或不再滚动报错，就说明起来了。
**第四步**：打开浏览器（Chrome / Edge 都行），在地址栏输入：
```
http://127.0.0.1:8000
```
**第五步（登录）**：如果网页跳出登录框，输入：
- 账号：`admin`
- 密码：`admin123`
如果**直接进去了没让登录**，也正常（当前设置里登录校验是关闭的）。

**启动成功的判断标准**：
- 浏览器能打开上面地址，并且看到「机房列表」页面；
- 或浏览器打开 `http://127.0.0.1:8000/api/health` 显示一行包含 `"env"` 的文字（这是健康检查页）。

> 如果双击 start.bat 后提示「**端口 8000 已被占用**」，说明系统其实**已经在运行了**，直接打开浏览器访问上面的地址即可，不用再启动。

### 2. 停止系统
**第一步**：打开 `rack_system` 文件夹，双击 `stop.bat`。
**第二步**：它会自动找到占用 8000 端口的进程并关掉，窗口提示「已尝试停止」。
**确认已停止的标准**：
- 再打开 `http://127.0.0.1:8000`，浏览器提示「无法访问」或「拒绝连接」；
- 或按 `Win+R` 输入 `cmd` 回车，在黑窗口里输入 `netstat -ano | findstr ":8000"`，**没有任何结果**就说明停干净了。

### 3. 重启系统
标准步骤：**先 `stop.bat`，再 `start.bat`**，中间隔几秒。
**什么情况下需要重启**：
- 系统更新（重装）之后；
- 网页改完内容刷新不出来；
- 页面能开但数据异常、导出报错；
- 觉得系统卡住、反应不对的时候。

> **重要**：任何「更新系统」操作后，都必须执行一次「停止 → 启动」让新内容生效。

---

## 四、日常运维工作清单

### 1. 数据备份（最重要）
- **数据库文件完整路径**：`C:\Users\Administrator\rack_system\backend\data\dcim.db`
- **备份方法（复制粘贴即可）**：
  1. 先按上面「停止系统」关掉服务（避免备份时文件正在被写入）；
  2. 打开 `backend\data\` 文件夹，右键 `dcim.db` → **复制**；
  3. 粘贴到备份目录（如 `D:\机柜系统备份\` 或移动硬盘），建议**按日期改名**保存，例如 `dcim_20260811.db`。
- **建议备份频率**：**每次更新系统前必做**；平时建议**每周至少一次**。
- **存放建议**：备份文件不要和原文件放同一个文件夹；最好放到别的磁盘或移动硬盘，防止原盘损坏一起丢。

### 2. 日志查看（看系统有没有报错）
- 系统运行时的报错信息，主要显示在 **`RackSystem-Backend` 那个黑色窗口**里。
- **怎么看**：启动后让那个窗口保持可见，出错时会刷红色/报错文字。
- **判断正常还是错误**：
  - 正常：窗口启动后稳定显示 `startup complete` 一类提示，之后基本不滚动；
  - 错误：窗口里出现 `Error`、`Traceback`、`Exception`、红色字，或启动后很快闪退关闭。
- 如果那个窗口已经关了，可以重新双击 `start.bat` 再观察。

### 3. 定期检查项
- **端口是否正常**：`cmd` 里输入 `netstat -ano | findstr ":8000"`，能看到 `LISTENING` 说明服务在跑。
- **服务是否在运行**：浏览器打开 `http://127.0.0.1:8000` 能进页面即正常。
- **磁盘空间是否充足**：`C` 盘剩余空间建议保持几 GB 以上，空间满了会导致系统写不下数据。

### 4. 版本更新
- **更新前必须做的准备**：
  1. **务必先备份数据库**（`backend\data\dcim.db`，见上）；
  2. 停止系统（`stop.bat`）。
- **常规更新步骤**（由开发人员提供新文件后）：
  1. 把新文件放到 `rack_system` 对应位置（覆盖旧文件）；
  2. 双击 `setup.bat` 重新初始化/构建（它会自动装依赖、重建网页、初始化数据库，已存在的数据不会丢）；
  3. 双击 `start.bat` 启动。
- **更新后验证**：
  1. 浏览器打开 `http://127.0.0.1:8000`，确认能进、能看到机房列表；
  2. 抽查一台设备，确认数据在；
  3. 试一次「导出全部设备」，确认能下载 Excel。

> **不确定怎么更新时，先备份、再找开发人员确认，不要自己覆盖文件。**

---

## 五、常见故障与排查解决

### 1. 浏览器打不开系统页面
**现象**：输入 `http://127.0.0.1:8000` 后，浏览器显示「无法访问此网站 / 拒绝连接」。
**分步排查**：
1. 先确认系统**有没有启动**：双击 `start.bat`，看有没有弹出 `RackSystem-Backend` 窗口。
2. 确认地址没打错：必须是 `127.0.0.1:8000`（数字 `1` 不是字母 `l`）。
3. 看 `start.bat` 窗口有没有报「端口 8000 已被占用」——如果有，说明系统其实**已经在跑**，直接刷新浏览器即可。
4. 若窗口一闪而过或报错，见下方第 2 条。
**解决**：按「三、启动系统」重新启动；若仍不行，收集窗口里的报错文字反馈给开发人员。

### 2. 启动脚本运行后闪退 / 弹窗报错
**现象**：双击 `start.bat` 后黑窗口瞬间关闭，或提示找不到 Python/Node。
**分步排查**：
1. 先运行一次 `setup.bat`，它会检查并安装所需环境（首次或环境异常时必做）。
2. 确认 `C:\Users\Administrator\.workbuddy\binaries\` 下的 Python、Node 运行环境**没有被删除或移动**（见二、2）。
3. 看弹出的黑窗口里最后几行红字是什么。
**解决**：多数情况运行 `setup.bat` 即可修复；若提示找不到运行环境目录，说明被误删，需找开发人员重装环境，**不要自己瞎改路径**。

### 3. Excel 导入设备失败
**现象**：在「机房列表页」或「机房详情页」点「📥 导入设备」，上传文件后提示错误或导入不成功。
**分步排查**：
1. **必须用系统给的模板**：在导入弹窗里先下载 `设备导入模板.xlsx`，不要自己新建表格。
2. 模板是**固定 14 列**，顺序不能乱、不能少列，依次为：
   `资源ID` / `资源编号` / `设备类型` / `品牌名称` / `型号` / `区域省份城市场地` / `机房编号/名称` / `机柜名称` / `机柜编号` / `起始U位` / `占用U位数` / `资产状态` / `SN序列号（可选）` / `主机名`
3. 导入前系统会先「预览校验」：**绿色**表示将通过，**红色**会列出具体哪一行错。
4. 常见错误原因：
   - 「起始U位」「占用U位数」填了超出机柜范围的数值（普通机柜 42U，U 位从 1 到 42）；
   - 同一机柜里 U 位和别的设备**打架（重叠）**，会提示「位置已占用告警」；
   - 「机房编号/名称」「机柜编号」在系统里不存在；
   - 「资产状态」填了非规定的值（正常应为「运行中」「已下架」等）。
**解决**：按红色提示改好对应行，再点「确认导入」。整批只要有一行错，系统会整批不写入，不用怕数据乱。

### 4. 页面能打开但数据不显示 / 加载不出来
**现象**：网页打开了，但列表空白、机柜图不显示、或一直转圈。
**分步排查**：
1. 看 `RackSystem-Backend` 窗口是不是被关了（关了就停服了）→ 重新 `start.bat`。
2. 刚更新完系统但没重启 → 先 `stop.bat` 再 `start.bat`。
3. 浏览器缓存了旧页面 → 按 **`Ctrl + F5`** 强制刷新。
4. 如果页面要求登录但你没登 → 用 `admin / admin123` 登录。
**解决**：按上面 1→2→3→4 顺序试，基本都能恢复。

### 5. 提示端口被占用
**现象**：启动或访问时提示 `端口 8000 已被占用`。
**分步排查与解决**：
- **如果是本系统自己占的**（之前没正常关）：双击 `stop.bat` 释放端口，再 `start.bat`；或直接打开 `http://127.0.0.1:8000` 访问（它其实在跑）。
- **如果是别的程序占用了 8000 端口**：`stop.bat` 后再启动仍失败，则需要换端口，这要**找开发人员修改启动脚本**，不要自己乱改。

### 6. 误删数据怎么从备份恢复
**现象**：不小心把设备下架、或数据不对，想恢复。
**分步排查与解决**：
- **只是「下架」错了**：不用备份。进「机房详情页」找到「已下架设备」折叠面板，点「重新上架」即可（如果该 U 位被别的设备在用会提示「位置已占用告警」，先把占用设备下架再重上架）。
- **真把数据弄没了 / 数据库损坏**：用备份恢复——
  1. 先 `stop.bat` 停止系统；
  2. 把备份的 `dcim.db`（如 `dcim_20260811.db`）**复制回** `C:\Users\Administrator\rack_system\backend\data\`，并重命名为 `dcim.db` 覆盖原文件；
  3. 再 `start.bat` 启动。
  > **恢复会覆盖成备份时的状态**，备份之后的新改动会丢失，所以备份越新越好。

---

## 六、安全与红线注意事项

### 1. 绝对不能删除 / 修改的文件和目录
- **`backend\data\dcim.db`**：数据库，所有业务数据都在里面对，删了全没。
- **`backend\.env.local`**：配置文件，里面写着登录密码和系统密钥，乱改会导致系统起不来或泄露。
- **`start.bat` / `stop.bat` / `setup.bat`**：启动停止脚本，勿改。
- **`C:\Users\Administrator\.workbuddy\binaries\…`**：系统运行环境，在 rack_system 外面，**绝对不能删**。
- 整个 `rack_system\web\dist\` 网页文件：由脚本自动生成，不要手改。

### 2. 账号密码管理规范
- 当前默认账号 `admin` / 密码 `admin123` **过于简单**，仅适合本机测试。
- **正式环境强烈建议修改默认密码**；修改方式涉及系统设置，**请找开发人员协助**，不要自己改 `.env.local` 里的密码字段（那只在第一次建库时生效）。
- 账号密码不要写在便签、群聊里明文传播。

### 3. 数据安全注意事项
- **改任何东西前先备份** `dcim.db`（见四、1）。
- 不要用 Excel、数据库工具直接打开 `dcim.db` 去手动改数据，极易损坏文件。
- 备份文件单独存放，别和原文件放一起。

### 4. 哪些操作必须先找开发人员确认（不能自己来）
- 修改访问端口（8000）、修改配置文件内容；
- 系统版本升级、替换程序文件；
- 改动数据库结构或任何代码；
- 把系统从本机搬到别的服务器 / 改成本地网络对外访问；
- 任何你拿不准、手册没写到的操作。

---

## 七、附录

### 1. 默认访问地址与默认账号
- **访问地址**：`http://127.0.0.1:8000`（本机浏览器打开）
- **默认账号**：`admin`
- **默认密码**：`admin123`
- **健康检查地址**（用来确认系统活着）：`http://127.0.0.1:8000/api/health`
- **Excel 模板下载**：在网页「导入设备」弹窗里直接下载；或访问 `http://127.0.0.1:8000/api/files/template`
- **示例数据下载**：`http://127.0.0.1:8000/api/files/sample`

> 注：当前配置里登录校验是关闭状态，所以可能不弹登录框直接进；账号仍保留，开启校验后即需登录。

### 2. 无法解决时，收集这些信息反馈给开发人员
1. **你做了什么操作**（比如：双击 start.bat / 导入某个 Excel / 更新后重启）；
2. **`RackSystem-Backend` 黑窗口里的报错文字**（整段复制，最好截图）；
3. **浏览器现象截图**（白屏、报错提示等）；
4. **端口占用情况**：`cmd` 里跑 `netstat -ano | findstr ":8000"` 的结果；
5. **系统版本**：看 `rack_system\CHANGELOG.md` 最上面写的版本号（当前为 `1.3.0`）；
6. **是否做过备份**、备份文件在哪。

带上以上信息，开发人员能最快定位问题。

---

> 文档维护说明：本手册内容依据 `README.md`、`start.bat` / `stop.bat` / `setup.bat`、`backend/.env.example`、`backend/.env.local`、`backend/init_db.py`、`backend/app/main.py`、`CHANGELOG.md` 的真实配置生成。系统已配置**每周一自动核对**上述文件，若路径、端口、账号、脚本或故障排查说明有变动，将自动同步更新本文件。
