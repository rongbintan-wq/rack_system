# 机柜管理系统 · 开发手册（DCIM-Lite）

> 最后更新：2026-08-11
> 适用版本：v1.2.0（CHANGELOG 最新）
> 阅读对象：后端 / 前端 / AI 协作开发者
> 本手册所有路径、端口、命令均来自本仓库真实文件（`setup.bat`、`start.bat`、`backend/.env.*`、`app/main.py`、`package.json`、`requirements.txt`、`CHANGELOG.md`），请勿凭记忆修改。

---

## 一、技术栈与拓扑

| 层 | 技术 | 说明 |
|---|---|---|
| 后端 | FastAPI + SQLAlchemy 2.x + SQLite（初期） | 单进程 uvicorn，:8000 同时托管前端 `web/dist` |
| 前端 | Vue 3 + Vite 5 + Element Plus + Pinia + Vue Router | 生产构建产物 `web/dist` 由后端同端口托管 |
| 数据库 | SQLite（`backend/data/dcim.db`） | 后期切 MySQL 仅需改 `DATABASE_URL` |
| 鉴权 | JWT（python-jose）+ RBAC | `REQUIRE_AUTH=false` 时关闭校验（DEV 默认） |
| 导入导出 | openpyxl | Excel 解析 / 模板 / 示例生成 |

**端口约定**：后端 `:8000`；前端开发热更新 `:5173`（仅开发需要，已配 `/api` 代理到 `:8000`）。**API 根路径是 `/api`，不是 `/api/v1`**。

---

## 二、运行环境（Windows 本机，路径已写死）

本机使用受管（managed）运行时，安装在 `rack_system` 目录**之外**，被脚本以绝对路径引用：

| 运行时 | 绝对路径 | 版本 |
|---|---|---|
| Python venv | `C:\Users\Administrator\.workbuddy\binaries\python\envs\rack\Scripts\python.exe` | 3.13.12 |
| Node/npm | `C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2\npm.cmd` | 22.22.2 |

> ⚠️ **这两项运行时绝对不能删除 / 移动 / 修改**，否则 `setup.bat` / `start.bat` 全部失效。

### 依赖清单
- 后端 `requirements.txt`：`fastapi>=0.110`、`uvicorn[standard]>=0.29`、`sqlalchemy>=2.0`、`openpyxl>=3.1`、`pydantic-settings>=2.2`、`python-jose[cryptography]>=3.3`、`passlib[bcrypt]>=1.7` + **`bcrypt==4.0.1`（必须锁版本）**、`python-multipart>=0.0.9`、`httpx>=0.27`、`pytest>=8.0`。
- 前端 `web/package.json`：依赖 `vue`/`vue-router`/`pinia`/`element-plus`/`@element-plus/icons-vue`/`axios`；dev 依赖 `@vitejs/plugin-vue`/`vite`。脚本：`dev` / `build` / `preview`。

---

## 三、一键脚本（Windows，CMD 中运行）

| 脚本 | 作用 | 内部关键动作 |
|---|---|---|
| `setup.bat` | 初始化 / 重装 | 检查运行时 → 复制 `.env.example`→`.env.local`（若存在则跳过）→ `pip install -r requirements.txt` → `npm install` + `npm run build` → `python init_db.py` |
| `start.bat` | 启动后端 | 检测 8000 占用；未占用则 `start "RackSystem-Backend" python -m uvicorn app.main:app --host 0.0.0.0 --port 8000` |
| `stop.bat` | 停止后端 | `netstat` 找到 8000 监听 PID → `taskkill /PID /F` |

Linux/macOS 对应 `scripts/setup.sh`、`start.sh`（需自行把运行时放入 PATH，且未锁 `bcrypt` 版本）。

> 首次或环境异常时先跑 `setup.bat`。**改后端代码后必须 `stop.bat` 再 `start.bat` 重启**（详见 §7 重启铁律）。

---

## 四、本地启动（手动方式，便于调试）

**后端**（在 `backend/` 目录）：
```bat
cd C:\Users\Administrator\rack_system\backend
C:\Users\Administrator\.workbuddy\binaries\python\envs\rack\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --log-level info
```

**前端开发热更新**（另开终端，在 `web/` 目录）：
```bat
cd C:\Users\Administrator\rack_system\web
C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2\npm.cmd run dev
```
访问 `http://127.0.0.1:5173`，其 `/api` 已代理到 `:8000`，无跨域问题。

**生产访问**：`http://127.0.0.1:8000`（后端直接托管 `web/dist`，含 SPA 深链接回退）。

---

## 五、配置文件 `.env.local`

由 `backend/.env.example` 复制而来（已被 `.gitignore` 排除，**禁止入库**）。关键字段：

| 字段 | 默认值 | 说明 |
|---|---|---|
| `ENV` | `dev` | dev / prod |
| `DEBUG` | `true` | |
| `DATABASE_URL` | `sqlite:///./data/dcim.db` | 相对 `backend/`；切 MySQL 只改此处 |
| `SECRET_KEY` | `change-me-in-prod...` | **PROD 必须替换** |
| `JWT_ALGORITHM` | `HS256` | |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | `1440` | 24h |
| `DEFAULT_ADMIN_USERNAME` / `DEFAULT_ADMIN_PASSWORD` | `admin` / `admin123` | 仅 DEV 初始化写入，PROD 应通过注册/导入 |
| `REQUIRE_AUTH` | `false` | DEV 关校验；**PROD 必须为 true** |
| `CORS_ORIGINS` | `*` | PROD 填具体前端域名 |

配置加载：`app/config.py` 的 `Settings` 读取 `backend/.env.local`，`BASE_DIR` = `backend/`，`DATA_DIR` = `backend/data/`（自动建目录）。

---

## 六、目录结构与关键文件

```
rack_system/
├── AGENTS.md                # 项目规则（AI 自动读取，含数据模型/算法/规范）
├── README.md / CHANGELOG.md
├── setup.bat / start.bat / stop.bat
├── backend/
│   ├── app/
│   │   ├── main.py          # 入口：路由注册 / SPA 托管 / 异常归一化 / health / 文件下载
│   │   ├── config.py        # 配置与环境变量
│   │   ├── database.py      # 引擎 / SessionLocal / Base
│   │   ├── models.py        # ORM：Room / Rack / Device / ImportLog / AuditLog / User（软删+审计）
│   │   ├── schemas.py       # Pydantic 入参/出参
│   │   ├── security.py      # JWT + RBAC + hash_password
│   │   ├── crud.py          # 增删改唯一入口（软删 + 审计 + 占用口径统一）
│   │   ├── importer.py      # Excel 解析 + 冲突检测 + 事务
│   │   ├── excel_template.py# 模板 / 示例生成
│   │   ├── utils/svg.py     # SVG 坐标 + 颜色（与前端公式一致）
│   │   └── routers/         # rooms.py / racks.py / devices.py / import_.py / auth.py
│   ├── tests/importer_test.py  # pytest：U 位冲突算法（4 项）
│   ├── init_db.py           # 建表 + 预置 3 机房 / 6 机柜 / 15 设备 / 1 管理员
│   └── data/                # SQLite + 模板/示例 xlsx（dcim.db 已被 gitignore）
└── web/
    └── src/
        ├── api/             # axios 封装，baseURL='/api'
        ├── components/       # RackView / RackGrid / DeviceDrawer / ImportDialog / RoomCard / DeviceTypeChart
        ├── views/           # RoomsView / RoomDetailView / RackDetailView(深链)
        ├── router/ stores/ utils/svg.js  App.vue  main.js
```

---

## 七、后端开发约定（铁律）

1. **软删**：删除一律置 `is_deleted=True`，**禁止物理 DELETE**（含设备/机柜/机房）。
2. **统一返回**：`{ "code": 0, "data": {}, "msg": "success" }`，`code ≠ 0` 为错误；异常由 `main.py` 的 `HTTPException` / `Exception` handler 归一化。
3. **DB 访问走 `crud.py`**：API 层只调用 `crud`，不直接写 SQL；批量操作须 Dry-Run 预检；导入/导出用事务或流式。
4. **审计**：关键写操作写 `audit_log`（operator_id / 时间 / 前后值对比）。
5. **占用判定唯一口径**（四者一致：渲染 / 冲突 / 统计 / 列表）：
   ```
   占用 ⇔ is_deleted=False AND asset_status != '已下架'
   ```
   下架设备不渲染、不计入占用、不阻塞新上架；冲突提示语统一 **「位置已占用告警」**。
6. **下架语义 ≠ 软删**：下架仅置 `asset_status='已下架'`，保留 `is_deleted=False` 与原 U 位信息，以便「重新上架」复用原位置；重上架时若目标 U 被在用设备占用 → 返回「位置已占用告警」并禁止上架。
7. **路由顺序与重启铁律**（防 422 / 404）：
   - 静态动作路由（如 `/export`、`/preview`、`/decommissioned`）**必须声明在 `/{id}` 等路径参数路由之前**；更稳妥用独立前缀如 `/export/all`。
   - **改后端代码后必须重启 uvicorn**：旧进程持旧路由表，新端点不会生效（典型症状：刚加的 `/export` 返回 422，因为被 `/{device_id}` 误匹配为 `device_id="export"` 触发 int 解析失败）。
8. **API 路径**：根前缀 `/api`；`GET /api/health` 健康检查；`GET /api/files/template`、`/api/files/sample` 下载模板/示例。

---

## 八、前端开发约定

- 入口：`web/src/main.js` → `App.vue`（含路由出口 + Pinia）。
- API 封装：`web/src/api/index.js` 已设 `baseURL:'/api'`，**勿自行加 `v1`**。
- 路由：`/rooms` → `/rooms/:id`（机房详情，内联机柜矩阵）；`/racks/:id` 仅作可选深链。
- 状态：`stores/` 下 rooms / racks / devices Pinia store。
- 视图层级：机房列表（RoomsView）与机房详情（RoomDetailView，内联 SVG 机柜矩阵）两级，**无独立机柜详情菜单**。点击设备色块 → DeviceDrawer（查看/编辑/下架）；点击空闲 U → 上架表单。
- 设备色块渲染「品牌 + 型号」标签；颜色按 `utils/svg.js` 与 `app/utils/svg.py` 一致编码。
- 删除 / 下架前端二次确认。
- **构建产物带 content-hash**，改完前端必须 `npm run build` 后浏览器 **Ctrl+F5 强刷**（否则缓存旧 JS 导致白屏）。

---

## 九、核心算法（已用 pytest 覆盖）

1. **SVG 坐标公式**（U1 在底部，Y 轴向下，含 +1 修正）：
   ```
   设备块顶边 Y = 10 + (rack.height_u - start_u - dev_height_u + 1) × 22
   设备块高度   = dev_height_u × 22
   ```
   42U 机柜、start_u=10、height_u=2 → Y=692、高=44（已验证）。常量见 `app/utils/svg.py`（`U_HEIGHT=22`、`TOP_MARGIN=10`、`RACK_WIDTH=140`）。
2. **U 位冲突检测**：区间交集 `max(start_a, start_b) <= min(end_a, end_b)`；仅比对 `is_deleted=False AND asset_status!='已下架'`；**同资源编号本次将原地 UPDATE 的旧记录须排除**（否则自冲突导致整批回滚）。
3. **下架语义**：见 §7.6。

---

## 十、数据库与初始化

- 文件：`backend/data/dcim.db`（SQLite，已被 `.gitignore` 排除）。
- `init_db.py`：`Base.metadata.create_all` 建表 → 幂等预置 3 机房（开放区01/02、荣耀机房）/ 6 机柜（A01/A02/B01/B02/C01/C02，均 42U）/ 15 设备 / 1 管理员（`admin/admin123`）。重复执行不会重复插入。
- **数据备份**：停止服务后直接复制 `dcim.db`；恢复时覆盖回 `backend/data/dcim.db`。
- 切 MySQL：仅改 `.env.local` 的 `DATABASE_URL`（`sqlalchemy` 连接串），其余代码不变。

---

## 十一、测试

后端单测（`backend/tests/importer_test.py`，4 项，覆盖 U 位冲突算法）：
```bat
cd C:\Users\Administrator\rack_system\backend
C:\Users\Administrator\.workbuddy\binaries\python\envs\rack\Scripts\python.exe -m pytest tests/ -q
```
用例：`test_u_conflict_detected`（冲突检测）、`test_self_update_excluded`（排除自身 UPDATE）、`test_u_out_of_bounds`（越界）、`test_missing_rack`（机柜不存在）。测试使用临时 SQLite，不污染业务库。

前端：当前 `package.json` 未配置 lint/test 脚本；构建校验用 `npm run build`（通过即视为 OK）。

---

## 十二、Git 与发布流程（见 AGENTS.md 第九章）

- **手动确认铁律**：所有 git 写操作（commit / tag / push / merge / 分支删除 / 建 PR / 合 PR）**必须等用户明确确认**；只读命令（`status`/`log`/`diff`）可直接执行。
- **分支模型**：`main` 是唯一远程根分支（无 master）；开发切短生命周期分支（`feat/xxx`、`fix/xxx`），经 PR 合回 `main` 后删除源分支。
- **PR 流程**：`git fetch` → `git log --oneline origin/main..<branch>` → `git diff --stat origin/main...<branch>` 自检 → GitHub 建 PR（`base=main`）→ 优先 **Squash and merge** → Delete branch → 本地 `git branch -d` + `git fetch --prune`。
- **版本标签**：`git tag -a vX.Y.Z -m "..."` → `git push origin vX.Y.Z`（仅推 main 及其 tag）。
- 本仓库已固化 `github-api-pr` 技能（取凭据 → 建 PR → 合 PR → 清分支），无 `gh` CLI 时复用。

> 版本一致性：`app/main.py` 的 `FastAPI(version="1.2.0")` 与 `web/package.json` 的 `"version":"1.2.0"` 已与 `CHANGELOG.md` 最新 `1.2.0` 对齐。

---

## 十三、常见问题（排错，节选自 README 第八节）

1. **白屏**：`App.vue` 的 `<script setup>` 用 `ref()` 但未 `import { ref } from 'vue'` → Vue 无法挂载。修复后 `npm run build` + Ctrl+F5。
2. **前端改完没变化**：content-hash 缓存，Ctrl+F5 或清缓存。
3. **`npm run build` 清 dist 报 safe-delete / trash 错误**（受管沙箱拦截 `rmSync`）：手动删 `web/dist` 再构建：
   ```bat
   powershell -Command "Remove-Item -LiteralPath 'C:\Users\Administrator\rack_system\web\dist' -Recurse -Force"
   npm run build
   ```
4. **接口 404（`/api/v1/...`）**：根路径是 `/api`，非 `/api/v1`。
5. **CMD 里 `bash scripts/setup.sh` 报 "bash 不是内部命令"**：Windows 用 `setup.bat` / `start.bat` / `stop.bat`，`.sh` 仅供 Linux/macOS。
6. **端口被占用**：后端已在运行，直接开 `http://127.0.0.1:8000`；需重启先 `stop.bat` 再 `start.bat`。
7. **设备导出 422**：旧 uvicorn 进程无 `/export` 路由 → `stop.bat` + `start.bat` 重启加载新路由表。

---

## 附录 A：路径 / 端口速查

| 项 | 值 |
|---|---|
| 项目根 | `C:\Users\Administrator\rack_system` |
| 数据库 | `backend/data/dcim.db` |
| 配置文件 | `backend/.env.local` |
| 后端地址 | `http://127.0.0.1:8000` |
| 前端开发地址 | `http://127.0.0.1:5173` |
| 健康检查 | `GET /api/health` |
| 模板/示例下载 | `GET /api/files/template`、`GET /api/files/sample` |
| 主要 API | `GET /api/rooms`、`GET /api/racks`、`GET /api/devices`、`POST /api/import/preview`、`POST /api/import/commit`、`GET /api/devices/export` |
| Python 运行时 | `C:\Users\Administrator\.workbuddy\binaries\python\envs\rack\Scripts\python.exe` |
| Node 运行时 | `C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2\npm.cmd` |

## 附录 B：版本

- `v1.0.0` (2026-08-10)：Phase 1 全栈骨架 + 导入 + SVG 视图 + pytest 4 项。
- `v1.1.0` (2026-08-11)：资源模型收敛为三级（删 Port/Connection）。
- `v1.2.0` (2026-08-11)：统一下架占用口径、重上架闭环、冲突提示统一、导出 422 修复。
